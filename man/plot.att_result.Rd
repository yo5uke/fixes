% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/att.R
\name{plot.att_result}
\alias{plot.att_result}
\alias{autoplot.att_result}
\title{Plot an ATT aggregation result}
\usage{
\method{plot}{att_result}(
  x,
  ci_level = 0.95,
  color = "#B25D91FF",
  zero_line = TRUE,
  theme_style = c("bw", "minimal", "classic"),
  ...
)

\method{autoplot}{att_result}(object, ...)
}
\arguments{
\item{x}{An \code{att_result} object.}

\item{ci_level}{Confidence level to display (default \code{0.95}; must be one
of the levels requested in the originating call).}

\item{color}{Point and interval colour (default \code{"#B25D91FF"}).}

\item{zero_line}{Logical; draw a dashed reference line at zero
(default \code{TRUE}).}

\item{theme_style}{One of \code{"bw"} (default), \code{"minimal"}, or \code{"classic"}.}

\item{...}{Unused.}

\item{object}{An \code{att_result} object.}
}
\value{
A \code{ggplot} object.
}
\description{
Base \code{plot()} method for \code{att_result} objects returned by \code{\link[=att]{att()}} (or the
deprecated \code{\link[=calc_att]{calc_att()}}). Draws point estimates with confidence-interval
bars: a single point for \code{aggregation = "simple"}, one point per cohort
for \code{"by_cohort"}, and one per calendar period for \code{"by_time"}.
}
\examples{
\dontrun{
res <- att(df, outcome = y, time = year, timing = g, unit = id,
           aggregation = "by_cohort")
plot(res)
}

}
\seealso{
\code{\link[=att]{att()}}
}
