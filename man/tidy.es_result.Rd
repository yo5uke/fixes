% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy-es_result.R
\name{tidy.es_result}
\alias{tidy.es_result}
\title{Tidy an \code{es_result} object}
\usage{
\method{tidy}{es_result}(x, ...)
}
\arguments{
\item{x}{An \code{es_result} object.}

\item{...}{Unused; kept for compatibility with the \code{\link[broom:tidy]{broom::tidy()}} generic.}
}
\value{
A tibble with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, \code{relative_time}, \code{is_baseline}, and the
\code{conf_low_XX}/\code{conf_high_XX} pairs of the originating call.
}
\description{
Returns the event-study coefficient table of an \code{\link[=event_study]{event_study()}} result as
a plain tibble — the format expected by \code{modelsummary::modelsummary()}
and other broom-aware tooling. The baseline row (zero by construction)
is included; drop it with \code{x[!x$is_baseline, ]} beforehand if undesired.
}
\examples{
\dontrun{
res <- event_study(df, outcome = y, time = year, timing = g,
                   unit = id, estimator = "cs")
broom::tidy(res)
}

}
