% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/att.R
\name{att}
\alias{att}
\title{ATT aggregation for staggered adoption designs}
\usage{
att(
  data,
  outcome,
  time,
  timing,
  unit,
  estimator = c("cs", "bjs"),
  aggregation = c("simple", "by_cohort", "by_time"),
  control_group = c("nevertreated", "notyettreated"),
  anticipation = 0L,
  conf_level = 0.95,
  interval = 1,
  time_transform = FALSE
)
}
\arguments{
\item{data}{A data.frame containing panel data.}

\item{outcome}{Unquoted outcome variable (name or expression, e.g., \code{log(y)}).}

\item{time}{Unquoted calendar time variable (numeric).}

\item{timing}{Unquoted column giving each unit's first treatment period
(\code{NA} = never treated).}

\item{unit}{Unquoted unit identifier (required).}

\item{estimator}{Estimation strategy: \code{"cs"} (Callaway-Sant'Anna 2021,
default) or \code{"bjs"} (Borusyak-Jaravel-Spiess 2024).}

\item{aggregation}{Aggregation type: \code{"simple"} (overall ATT, default),
\code{"by_cohort"} (one ATT per treatment cohort), or \code{"by_time"} (one ATT
per calendar time period).}

\item{control_group}{For \code{estimator = "cs"}: comparison group,
\code{"nevertreated"} (default) or \code{"notyettreated"}.}

\item{anticipation}{For \code{estimator = "cs"}: number of anticipation periods
before treatment (non-negative integer, default \code{0L}).}

\item{conf_level}{Numeric confidence level(s) (default \code{0.95}). Multiple
levels are supported, e.g., \code{c(0.90, 0.95)}.}

\item{interval}{Numeric time spacing (default \code{1}; informational only).}

\item{time_transform}{Logical; if \code{TRUE}, creates consecutive integer time
within unit via \code{dplyr::dense_rank()}. Requires \code{unit}.}
}
\value{
A \code{data.frame} of class \code{"att_result"} with columns:
\describe{
\item{\code{group}}{Cohort or calendar time (\code{NA} for \code{"simple"}).}
\item{\code{estimate}}{ATT point estimate.}
\item{\code{std.error}}{Standard error.}
\item{\code{statistic}}{t-statistic (\code{estimate / std.error}).}
\item{\code{p.value}}{Two-sided p-value (normal approximation).}
\item{\code{conf_low_XX}, \code{conf_high_XX}}{CI bounds for each \code{conf.level}.}
}

Attributes: \code{aggregation}, \code{estimator}, \code{conf.level}, \code{N}, \code{N_units},
\code{N_treated}, \code{N_nevertreated}, \code{control_group} (CS only), \code{att_gt} (CS
raw ATT(g,t) table), \code{tau_it} (BJS unit-time effects table).
}
\description{
Estimates average treatment effects on the treated (ATT) using either the
Callaway-Sant'Anna (2021) or Borusyak-Jaravel-Spiess (2024) estimator,
aggregated to a single summary effect (\code{"simple"}), per-cohort effects
(\code{"by_cohort"}), or per calendar-time effects (\code{"by_time"}).
}
\details{
This function complements \code{\link[=event_study]{event_study()}}: use \code{event_study()} when you
want a full event-study curve (dynamic effects by relative time), and
\code{att()} when you want aggregated ATT estimates that collapse the time
dimension.

The result has \code{broom::tidy()} and \code{broom::glance()} methods, so it can be
passed to \code{modelsummary::modelsummary()} (rendered with \code{tinytable}) to build
a publication table — including stacking several estimators (e.g. \code{\link[=did]{did()}}
TWFE, CS, and BJS) side by side in one table.

\code{att()} supersedes \code{\link[=calc_att]{calc_att()}} as of fixes 1.0.0.
}
\section{Aggregation formulas (CS estimator)}{

\itemize{
\item \strong{simple}: \eqn{\theta = \sum_g (n_g/n_{treated}) \cdot \overline{ATT(g,\cdot)}}
where \eqn{\overline{ATT(g,\cdot)}} is the mean over post-treatment periods.
\item \strong{by_cohort}: \eqn{\theta(g) = \overline{ATT(g,\cdot)}} per cohort.
\item \strong{by_time}: \eqn{\theta(t) = \sum_{g \le t} w(g,t) \cdot ATT(g,t)}
with \eqn{w(g,t) = n_g / \sum_{g' \le t} n_{g'}}.
}
}

\section{Standard errors (BJS estimator)}{

BJS SEs are approximate (naive sample variance of unit-time effects).
Cluster-robust SEs for BJS aggregations are planned for a future release.
}

\examples{
\dontrun{
att(df, outcome = y, time = year, timing = g, unit = id)
att(df, outcome = y, time = year, timing = g, unit = id,
    estimator = "bjs", aggregation = "by_cohort")
}

}
\seealso{
\code{\link[=event_study]{event_study()}} for event-study (dynamic) estimates, and
\code{\link[=calc_att]{calc_att()}} for the deprecated verb-style predecessor.
}
