% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/att_gt.R
\name{att_gt}
\alias{att_gt}
\title{Extract the ATT(g,t) table from a Callaway-Sant'Anna result}
\usage{
att_gt(x)
}
\arguments{
\item{x}{An \code{es_result} or \code{att_result} object estimated with
\code{estimator = "cs"}.}
}
\value{
A data frame of class \code{c("att_gt_result", "data.frame")} with
columns \code{g} (cohort), \code{t} (calendar time), \code{estimate}, and \code{std_error}.
}
\description{
Returns the cohort-by-period ATT(g,t) table computed by
\code{event_study(estimator = "cs")} or \code{att(estimator = "cs")} as a real
\code{att_gt_result} object with its own \link[=plot.att_gt_result]{plot()} and
\code{autoplot()} methods. Bootstrap metadata (when the originating call used
\code{bootstrap = TRUE}) is carried along so simultaneous-inference overlays
keep working.
}
\examples{
\dontrun{
res <- event_study(df, outcome = y, time = year, timing = g,
                   unit = id, estimator = "cs")
gt <- att_gt(res)
plot(gt)                  # heatmap
plot(gt, type = "facet")  # one panel per cohort
}

}
\seealso{
\code{\link[=plot.att_gt_result]{plot.att_gt_result()}}, \code{\link[=event_study]{event_study()}}, \code{\link[=att]{att()}}
}
